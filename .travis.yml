sudo: true
dist: trusty

language: perl

install:
  - ./.travis/install-dependencies.sh

script:
  - ./functional_tests/bionitio-test.sh -p $TRAVIS_BUILD_DIR/bionitio.pl -d functional_tests/test_data
  - ./.travis/unit-test.sh
